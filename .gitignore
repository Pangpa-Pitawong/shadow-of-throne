# ── Local / editor config (เครื่องส่วนตัว — ไม่ขึ้น repo) ──
/.claude/
.vscode/
.idea/
.DS_Store

# ── Node ──
node_modules/
dist/

# ── Asset-generation tooling (รันในเครื่อง ไม่ต้องขึ้น repo เกม) ──
/blender_*.py
/generate-*.js

# ── Generated / unused art assets ──
/renders/
/lobby/public/models3d/
/lobby/public/tiles/
# หมายเหตุ: lobby/public/models/gltf ใช้ runtime แล้ว (แมพ 3D IslandMap3D) → ไม่ ignore
